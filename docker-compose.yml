version: '3.8'

services:
  postgres:
    image: postgres:13
    container_name: crud_book_db
    env_file:
      - ./.env
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
